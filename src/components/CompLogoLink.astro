---
// Import the necessary script
const { footerVariant } = Astro.props;
---

<a href="">
  <img class="site-logo" src={`images/logo-${footerVariant === "secondary" ? "/dark" : "light"}.svg`} alt="" draggable="false"/>
  <span class="u-vh">Go to Modern Art Gallery homepage</span>
</a>

<style lang="scss">
  @use "../styles/10-functions/theme-accessor" as *;

  .site-logo {
    block-size: get(font-size, 4);
  }
</style>